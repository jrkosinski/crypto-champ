const env = 'LOCAL';

switch(env) {
    case 'LOCAL': 
        window.config = {
        };
        break;
        
    case 'DEV':  
        window.config = {
        };
        break;

    case 'PROD': 
        window.config = {
        };
        break;
}

